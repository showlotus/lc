/**
 * @param {number[]} coins
 * @param {number} amount
 * @return {number}
 */
var coinChange = function (coins, amount) {

};

var coins = [1, 2, 5]
var amount = 11

var res = coinChange(coins, amount)